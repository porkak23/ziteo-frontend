node_modules/
# Ignora TODOS los archivos de entorno (incl. .env.server, .env.production, .env.local)
# excepto las plantillas .env.example, que sí deben versionarse como documentación.
.env
.env.*
!.env.example
dist/
build/
.DS_Store
*.log
.vite/
.vercel/
# Graphify generated artifacts (graph.json ~2MB, graph.html ~2MB)
graphify-out/
